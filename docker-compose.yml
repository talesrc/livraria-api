version: '3.7'

services:
    db:
      image: mariadb
      container_name: livraria_db
      environment:
        MARIADB_ROOT_PASSWORD: senha1234
        MARIADB_DATABASE: LIVRARIA
      ports:
        - 3306:3306